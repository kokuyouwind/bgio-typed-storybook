import Client from './Client'
import type { Meta, ComponentStoryObj } from '@storybook/react'

const meta: Meta<typeof Client> = { component: Client }
export default meta

export const Start: ComponentStoryObj<typeof Client> = {}
